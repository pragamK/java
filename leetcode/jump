class Solution {
    public boolean canJump(int[] nums) {
       int i= 0, maxReach=0;
    while(i<nums.length && i<=maxReach){
        maxReach = max(i + nums[i], maxReach);
        i++;
    }
    if(i == nums.length)
        return true;
    return false;
    }
}
